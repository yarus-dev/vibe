{
  "name": "@app/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "source": "src/api/graphql.js",
  "main": "api/graphql.js",
  "scripts": {
    "clean": "rm -rf .build .turbo node_modules",
    "dev": "nodemon",
    "build": "node scripts/build",
    "start": "node .build/api/graphql",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js,.jsx --fix source"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0"
  },
  "devDependencies": {
    "@graphql-tools/optimize": "^2.0.0",
    "@luckycatfactory/esbuild-graphql-loader": "^3.8.1",
    "esbuild": "^0.20.1",
    "graphql": "^16.8.1",
    "graphql-tag": "^2.12.6",
    "nodemon": "^3.1.0"
  }
}
