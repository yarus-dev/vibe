{
  "name": "@app/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .build && rm -rf node_modules && rm -rf .turbo",
    "dev": "nodemon src/server.js",
    "build": "esbuild src/server.js --bundle --platform=node --format=esm --outdir=.build --outbase=src",
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js,.jsx --fix source"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "esbuild": "0.20.1",
    "nodemon": "^3.1.0"
  }
}
