export const Query = {
  hello () {
    return 'world'
  }
};

// export const Mutation = {};

// export const Subscription = {};

export const resolvers =  {
  Query,
  // Mutation,
  // Subscription
};

export default  resolvers;
